# Optional: path to CSV of cleaned restaurant rows (from Phase 2)
# CLEANED_DATA_PATH=./data/cleaned.csv

# Optional: CORS origins (comma-separated). Default: * 
# CORS_ORIGINS=http://localhost:3000

# GROQ_API_KEY: Phase 4 loads it from phase3/.env automatically for E2E.
# Put GROQ_API_KEY in phase3/.env — no need to duplicate here.
