# Hugging Face token (optional for public datasets; required for gated/private).
# Create at: https://huggingface.co/settings/tokens
# HF_TOKEN=your_token_here

# Optional: cache directory (defaults to phase1/data/hf_cache when running tests)
# HF_HOME=./data/hf_cache
